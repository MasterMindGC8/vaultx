{
  "windows": {
    "version": "1.6.1",
    "installer_url": "https://github.com/MasterMindGC8/vaultx/releases/download/v1.6.1/VaultXSetup-1.6.1.exe",
    "notes": "Fixed: the decoy/duress PIN failing with ACCESS DENIED the first time you tried it right after setup (needed a restart before). Fixed a debug-mode chat screen crash and a toolbar that could overflow on a narrow window."
  },
  "macos": {
    "version": "1.6.1",
    "installer_url": "https://github.com/MasterMindGC8/vaultx/releases/download/v1.6.1/VaultX-macOS.zip",
    "notes": "Fixed: the decoy/duress PIN failing with ACCESS DENIED the first time you tried it right after setup (needed a restart before). Fixed a debug-mode chat screen crash and a toolbar that could overflow on a narrow window. Unsigned build — right-click the app and choose Open the first time to get past Gatekeeper."
  },
  "linux": {
    "version": "1.6.1",
    "installer_url": "https://github.com/MasterMindGC8/vaultx/releases/download/v1.6.1/VaultX-linux-x86_64.tar.gz",
    "notes": "Fixed: the decoy/duress PIN failing with ACCESS DENIED the first time you tried it right after setup (needed a restart before). Fixed a debug-mode chat screen crash and a toolbar that could overflow on a narrow window. Extract the tarball and run the client_app executable inside bundle/."
  }
}
