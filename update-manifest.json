{
  "windows": {
    "version": "1.6.6",
    "installer_url": "https://github.com/MasterMindGC8/vaultx/releases/download/v1.6.6/VaultXSetup-1.6.6.exe",
    "notes": "Chat fixes, online status, sent/received confirmations, Retry, reconnect, local history toggle and relay latency. Both people should update to 1.6.6. Two ticks mean received by the app, not read by a person. Offline messages may need Retry after reopening."
  },
  "macos": {
    "version": "1.6.6",
    "installer_url": "https://github.com/MasterMindGC8/vaultx/releases/download/v1.6.6/VaultX-macOS.zip",
    "notes": "Chat fixes, online status, sent/received confirmations, Retry, reconnect, local history toggle and relay latency. Both people should update to 1.6.6. Two ticks mean received by the app, not read by a person. Offline messages may need Retry after reopening. Unsigned macOS build; right-click the app and choose Open on first launch."
  },
  "linux": {
    "version": "1.6.6",
    "installer_url": "https://github.com/MasterMindGC8/vaultx/releases/download/v1.6.6/VaultX-linux-x86_64.tar.gz",
    "notes": "Chat fixes, online status, sent/received confirmations, Retry, reconnect, local history toggle and relay latency. Both people should update to 1.6.6. Two ticks mean received by the app, not read by a person. Offline messages may need Retry after reopening. Extract the archive and run client_app inside bundle/."
  }
}
